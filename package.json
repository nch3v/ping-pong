{
  "name": "ping-pong",
  "version": "1.0.0",
  "description": "POC to have two services call each other to keep them alive",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "micro -p ${PORT:-3000}"
  },
  "keywords": [],
  "author": "Nicolas Chevolot",
  "license": "ISC",
  "dependencies": {
    "isomorphic-fetch": "^2.2.1",
    "micro": "^7.3.3"
  }
}